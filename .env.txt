# إعدادات الخادم
PORT=3000
NODE_ENV=development

# قاعدة البيانات (اختياري - إذا لم يكن لديك MongoDB سيستخدم الذاكرة)
MONGODB_URI=mongodb://localhost:27017/circl_chat

# للمستخدمين المتقدمين:
# JWT_SECRET=your_super_secret_key_here
# CORS_ORIGIN=http://localhost:3000
